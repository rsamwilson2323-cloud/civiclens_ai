# ── Client (safe to expose, Vite requires the VITE_ prefix) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── Server only — NEVER prefix with VITE_, never ship to the browser ──
GEMINI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
